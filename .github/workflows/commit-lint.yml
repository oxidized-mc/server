name: Commit Lint

on:
  pull_request:
    branches: [main]

jobs:
  lint:
    uses: oxidized-mc/ci/.github/workflows/commit-lint.yml@main
    with:
      scopes: "nbt,macros,protocol,world,game,server,types,ci,deps"

